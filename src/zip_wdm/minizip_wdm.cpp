    // #include <minizip/zip.h>
    // zipFile zf = zipOpen("test.zip", APPEND_STATUS_CREATE);
    // if (zf == nullptr) {
    //     return -1;
    // }

    // zip_fileinfo zi;
    // memset(&zi, 0, sizeof(zi));
    // zipOpenNewFileInZip(zf, "input.txt", &zi, nullptr, 0, nullptr, 0, nullptr, Z_DEFLATED, Z_DEFAULT_COMPRESSION);

    // const char *data = "adsfdbghuiu8sfsdfdgfjasd";
    // zipWriteInFileInZip(zf, data, strlen(data));
    // zipCloseFileInZip(zf);
    // zipClose(zf, nullptr);